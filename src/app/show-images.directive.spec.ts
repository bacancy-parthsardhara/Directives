import { ShowImagesDirective } from './show-images.directive';

describe('ShowImagesDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowImagesDirective();
    expect(directive).toBeTruthy();
  });
});
